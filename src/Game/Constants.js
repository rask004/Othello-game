class Constants {
    static emptySpace = Symbol("empty space")

    static counterRound = Symbol("round counter")
    static counterSquare = Symbol("square counter")

    static winningSequenceLength = 5

    static minLengthCheckingValidMoves = 3

    static boardWidth = 8
    static boardHeight = 8
}

export default Constants;